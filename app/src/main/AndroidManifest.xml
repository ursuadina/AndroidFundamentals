<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andreea.androidfundamentals">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".week5.PicassoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".week4.CCActivity" />
        <activity android:name=".week4.SpinnerActivity" />
        <activity android:name=".week4.CodeChallenge4Activity" />
        <activity android:name=".week4.CodeChallenge3Activity" />
        <activity android:name=".week4.RecyclerViewActivity" />
        <activity android:name=".week5.SecondActivity"></activity>
    </application>

</manifest>